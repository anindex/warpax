# Performance benchmarks

The [benchmark guide](../docs/reference/benchmarks.md) owns the asv catalog,
commands, environment matrix and interpretation. This harness is separate
from the public `warpax.benchmarks` module of reference spacetimes.
